<h2>Register</h2>

<!-- create into a validation_errors(object) helper -->

<% if @user.errors.any? %>
  <h3>There <%= @user.errors.count == 1 ? 'was' : 'were' %> <%= pluralize(@user.errors.count, "error") %>:</h3>
  <ul> 
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul> 
<% end %>

<%= link_to "Continue with Facebook", "/auth/facebook" %>

<br> <br> or <br><br>
<!-- create partial for reuse in edit_user -->

<%= form_for @user do |f| %>

  <%= f.text_field :username, placeholder: "Username" %> 
  <%= f.email_field :email, placeholder: "Email (optional)" %> 
  <%= f.password_field :password, placeholder: "Password" %> 
  <%= f.password_field :password_confirmation, placeholder: "Password Confirmation" %> 
  <br>
  <%= f.submit "Register" %>
<% end %>

<p>Already a member? <%= link_to "Log in.", '/home/#login' %></p>