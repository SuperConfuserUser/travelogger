<!--==========================
  Main
============================-->
<% if current_page?('/') %>
  <%= yield %>
  <div class="push"></div>
<% else %>
  <main>
    <%= yield %>
  </main>

  <div class="push"></div>
<% end %>

<!-- can do a helper for the if statement, but not sure how do yield with main tags for else -->